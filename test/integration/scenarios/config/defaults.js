// The whole integration test takes longer than the default timeout, so we give karma-webpack
// a generous 30 seconds to run.
// eslint-disable-next-line import/prefer-default-export
export const KARMA_SERVER_TIMEOUT = 30 * 1000;
